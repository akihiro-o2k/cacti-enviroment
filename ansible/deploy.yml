---

- hosts: all
  vars_files:
    - vars/{{ stage }}.yml
  roles:
    - linux-base
  tags:
    - linux-base

- hosts: cacti
  vars_files:
    - vars/{{ stage }}.yml
    - vars/common.yml
  roles:
    - httpd
#    - mariadb
#    - ntp-client
#  tags:
#    - httpd
#    - mariadb
#    - ntp-client
