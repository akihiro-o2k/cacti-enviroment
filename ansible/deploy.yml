---

- hosts: all
  vars_files:
    - vars/{{ stage }}.yml
    - vars/common.yml
  roles:
    - linux-base
  tags:
    - linux-base

- hosts: cacti01
  vars_files:
    - vars/{{ stage }}.yml
    - vars/common.yml
  roles:
    - httpd
    - db
    - cacti
    - spine
  tags:
    - httpd
    - db
    - cacti
    - spine
- hosts: cacti02
  vars_files:
    - vars/{{ stage }}.yml
    - vars/common.yml
  roles:
    - httpd
    - db
    - cacti
  tags:
    - httpd
    - db
    - cacti
