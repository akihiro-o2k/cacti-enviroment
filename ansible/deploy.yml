---

- hosts: all
  vars_files:
    - vars/{{ stage }}.yml
    - vars/common.yml
  roles:
    - linux-base
  tags:
    - linux-base

- hosts: cacti01
  vars_files:
    - vars/{{ stage }}.yml
    - vars/common.yml
  roles:
    - httpd
    - db
    - cacti
    - spine
    - edm
  tags:
    - httpd
    - db
    - cacti
    - spine
    - edm
- hosts: cacti02
  vars_files:
    - vars/{{ stage }}.yml
    - vars/common.yml
  roles:
    - httpd
    - db
    - cacti
    - spin
    - edm
  tags:
    - httpd
    - db
    - cacti
    - edm
